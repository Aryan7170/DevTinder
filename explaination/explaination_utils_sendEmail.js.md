# Explanation: utils/sendEmail.js

## Current state

This file is currently empty.

## Expected role (based on imports)

Other modules import this as `sendEmail` and call `sendEmail.run(subject, body)`.

## Exports

- None (empty file).

## Notes

Until this file exports the expected API, any runtime call like `sendEmail.run(...)` will fail.
